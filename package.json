{
  "name": "Movies",
  "version": "0.0.1",
  "description": "",
  "keywords": "",
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev & npm run build-css:watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass --include-path scss css/scss/main.scss -w css/custom.css"
  },
  "devDependencies": {
    "node-sass": "^5.0.0",
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "date-fns": "^2.16.1"
  }
}
